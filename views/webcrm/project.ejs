<% include include/main-header %>
<link rel="stylesheet" href="plugins/combobox/autocomplete-ui.css"/>
<link href="css/bootstrap-datetimepicker.min.css" type="text/css" rel="stylesheet">
<!-- page specific css -->
<link rel="stylesheet" href="css/basictable.css">
<link rel="stylesheet" href="css/autocomplete.css">
<link rel="stylesheet" href="css/jquery-ui.css">

</head>
<style>
.box-primary h4{
margin: 7px 5px 0px 0px;
border-radius: 3px;
padding: 9px;
border-bottom: 1px solid #ddd;
font-size: 20px;
}

#ProjectForm .custom-combobox{
  
      width: 351px;
    margin-left: 25px;
    top: 2px;
  }
  
  
  .ui-state-default, .ui-widget-content .ui-state-default, .ui-widget-header .ui-state-default {
    width: auto; 
    height: auto;
    line-height: 19px !important;
    background: #fafafa !important;
    padding: 7px 6px;
    border: 1px solid #ddd;
}
  
  
  #ProjectForm .ui-button {
    background: #fff;
    background: -moz-linear-gradient(top, #ffffff 0%, #f0f1f2 100%);
    background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,#ffffff), color-stop(100%,#f0f1f2));
    background: -webkit-linear-gradient(top, #ffffff 0%,#f0f1f2 100%);
    background: -o-linear-gradient(top, #ffffff 0%,#f0f1f2 100%);
    background: -ms-linear-gradient(top, #ffffff 0%,#f0f1f2 100%);
    background: linear-gradient(to bottom, #ffffff 0%,#f0f1f2 100%);
    filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#ffffff', endColorstr='#f0f1f2',GradientType=0 );
    display: inline-block;
    border: 1px solid #cfdde8;
   background-position: 94%;
    background-repeat: no-repeat;
    margin-left: -5px;
    padding: 0 6px;
    height: 35px;
}  
</style>
</head>
<body class="hold-transition skin-blue sidebar-mini">
	<div class="wrapper">
		<!-- Header Starts here-->
  		<% include include/header %>
  		<!-- Header Ends here-->
  		
  		<!-- Sidebar Starts Here-->
  			<% include include/sidebar %>
    	<!-- Sidebar Ends Here-->
    	
		<!-- Content Wrapper. Contains page content -->
  		<div class="content-wrapper">
    		<!-- Content Header (Page header) -->
    		<section class="content-header">
      			<h1> <% if (typeof contentObj.name !== 'undefined' && contentObj.name !== null) { %><%= contentObj.name %><% } else{	%>Add new<% }%><small>Project</small></h1>
      			<ol class="breadcrumb">
        			<% include include/dashboard-breadcrumb %>
        			<li class="active">Project</li>
      			</ol>
    		</section>
    		<!-- Main content -->
    		<section class="content">
				<div class="row">
					<section class="content">
						<div class="nav-tabs-custom">
								<% include include/display_returned_msg %>
            				<!-- Tabs within a box -->
           					<ul class="nav nav-tabs pull-left">
            					<li class="active"><a href="#first" data-toggle="tab">Details</a></li>
            					<% if (typeof contentObj._id !== "undefined" && contentObj._id !== null) { %>
           						<li><a href="#third" data-toggle="tab">Related Tasks</a></li>
           						<% } %>
							</ul>
            				<div class="tab-content ">
            					
              					<div class="chart tab-pane active" id="first" >
              					<form class="form-horizontal" action="<%= backendDirectory %>/save/project" method="POST" id="ProjectForm">
            						<input type="hidden" class="form-control" id="table_name" name="table_name" value="projects">
      								<input type="hidden" class="form-control" id="unique_field" name="unique_field" value="_id">
      								<input type="hidden" class="form-control" id="id" name="id" value="<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) { %><%= contentObj._id %><% } %>">
      								<input type="hidden" class="form-control" id="editorField" name="editorField" value="<% if (typeof editorField !== 'undefined' && editorField !== null) { %><%= editorField %><% }else{ %>_id<% } %>">
      								<input type="hidden" class="form-control" id="editorValue" name="editorValue" value="<% if (typeof editorValue !== 'undefined' && editorValue !== null) { %><%= editorValue %><% } %>">
            						<input type="hidden" class="form-control" id="data" name="data" value="">
            						<input name="notes" type="hidden" value="" id="notes">
            						
									<div class="row" STYLE="margin-top:10px;">
										<div class="col-md-6 col-xs-12">
          									<div class="box box-primary">
		    									<h4>Main Details</h4>
            									<div class="box-body">
														<div class="row">
           		  											<div class=" col-sm-12 col-lg-12">
   																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Client<sup class="required">*</sup></label>
    																<div class="col-sm-9 autocomplete">
      																	<select id="customer_mongo_id" name="customer_mongo_id"  CLASS="form-control">
      																	</select>
      																	
    																</div>
  																</div>
  																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Project name<sup class="required">*</sup></label>
    																<div class="col-sm-9">
      																	<input type="text" class="form-control" id="name" name="name" value="<% if (typeof contentObj.name !== 'undefined' && contentObj.name !== null) { %><%= contentObj.name %><% } %>">
    																</div>
  																</div>
																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">&nbsp;</label>
    																<div class="col-sm-3">
																		<label><input id="status" name="status" value="1" type="radio" <% if (typeof contentObj.status !== 'undefined' && contentObj.status !== null && contentObj.status == 1) { %>checked<% } %>> Active</label>
    			  													</div>
    			  													<div class="col-sm-3">
																		<label><input id="status" name="status" value="0" type="radio" <% if (typeof contentObj.status !== 'undefined' && contentObj.status !== null && contentObj.status == 0) { %>checked<% } %>> Completed</label>
    			  													</div>
  																</div>
  																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Stage <sup class="required">*</sup></label>
    																<div class="col-sm-9">
																		<select class="form-control" id="stage" name="stage">
																			<option value="Proposal" <% if (typeof contentObj.active !== 'undefined' && contentObj.active !== null && contentObj.active == "Proposal") { %>selected<% } %>>Proposal</option>
																			<option value="Development" <% if (typeof contentObj.active !== 'undefined' && contentObj.active !== null && contentObj.active == "Development") { %>selected<% } %>>Development</option>
																			<option value="Testing" <% if (typeof contentObj.active !== 'undefined' && contentObj.active !== null && contentObj.active == "Development") { %>selected<% } %>>Testing</option>
																		</select>
																	</div>
  																</div>
   															</div>
       			 										</div>
             									</div>
          									</div>
										</div>
										<div class="col-md-6 col-xs-12">
          									<div class="box box-primary">
		    									<h4>Important Dates</h4>
            									<div class="box-body">
														<div class="row">
           		  											<div class=" col-sm-12 col-lg-12">
   																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Start  <sup class="required">*</sup></label>
    																<div class="col-sm-9">
    																	<input type="hidden" class="form-control" id="start_timestamp" name="start_timestamp" value="<% if (typeof contentObj.start_timestamp !== 'undefined' && contentObj.start_timestamp !== null) { %><%= contentObj.start_timestamp %><% } %>">
    																	<div class='input-group date' id='start_datetime_picker'>
                    														<input type='text' class="form-control" id="start_date" value=""/>
                    														<span class="input-group-addon"><span class="glyphicon glyphicon-calendar"></span></span>
                														</div>	
    																</div>
  																</div>
															
  																<div class="form-group">
 																	<label for="" class="col-sm-3 control-label">Deadline</label>
 																	<div class="col-sm-9">
																		<input type="hidden" class="form-control" id="deadline_timestamp" name="deadline_timestamp" value="<% if (typeof contentObj.deadline_timestamp !== 'undefined' && contentObj.deadline_timestamp !== null) { %><%= contentObj.deadline_timestamp %><% } %>">
    																	<div class='input-group date' id='deadline_datetime_picker'>
                    														<input type='text' class="form-control" id="deadline_date" value=""/>
                    														<span class="input-group-addon"><span class="glyphicon glyphicon-calendar"></span></span>
                														</div>
																	</div>
																</div>
   															</div>
       			 										</div>
            									</div>
          									</div>
										</div>
									</div>
	  								<div class="row" STYLE="margin-top:10px;">
										<div class="col-md-6 col-xs-12">
          									<div class="box box-primary">
		    									<h4>Financial</h4>
            									<div class="box-body">
       		 											<div class="row">
           		  											<div class=" col-sm-12 col-lg-12">
   																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Budget</label>
    																<div class="col-sm-9">
      																	<input type="text" class="form-control" id="Budget" placeholder="Budget" name="Budget" value="<% if (typeof contentObj.Budget !== 'undefined' && contentObj.Budget !== null) { %><%= contentObj.Budget %><% } %>">
    																</div>
  																</div>
  																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Rate</label>
    																<div class="col-sm-9">
      																	<input type="text" class="form-control" id="Rate" placeholder="Rate" name="Rate" value="<% if (typeof contentObj.Rate !== 'undefined' && contentObj.Rate !== null) { %><%= contentObj.Rate %><% } %>">
    																</div>
  																</div>
																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Currency</label>
    																<div class="col-sm-9">
																		<select class="form-control" id="currency" name="currency">
																		</select>
																	</div>
  																</div>
  															</div>
       			 										</div>
												</div>
          									</div>
										</div>
										<div class="col-md-6 col-xs-12">
          									<div class="box box-primary">
		    									<h4>Handler</h4>
            									<div class="box-body">
														<div class="row">
           		  											<div class=" col-sm-12 col-lg-12">
   																<div class="form-group">
    																<label for="" class="col-sm-3 control-label">Incharge</label>
    																<div class="col-sm-9">
      																	<input type="text" class="form-control" id="project_incharge" name="project_incharge" placeholder="" value="<% if (typeof contentObj.project_incharge !== 'undefined' && contentObj.project_incharge !== null) { %><%= contentObj.project_incharge %><% } %>">
    																</div>
  																</div>
															
   															</div>
       			 										</div>
             									</div>
          									</div>
										</div>
									</div>
									
										<div class="col-md-12 col-lg-12" align="right">
               								<div class="submit-btn-setting"> <button type="submit" class="btn btn-primary btn-color margin-right-5 btn-sm">Submit</button>
               								<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) {	%>
               									<a href="<%= backendDirectory %>/list/projects" class="btn btn-danger btn-sm">Cancel</a>
               								<%	}else{	%>
               									<button type="reset" class="btn btn-danger btn-sm">Reset</button>
               								<% } %>
               								</div>
               							</div>
               						</form>
               						<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) { %>
              						<div class="box box-primary" style="margin-top:40px;">
		    							<h4>Notes</h4>
            							<div class="box-body">
											<div class="row">
               									<div class="col-sm-12" style="margin-top:20px;">
              										<a title="Add new note" onClick="$('#addNote').modal('show');" href="javascript:void(0)" class="btn btn-danger">Add new note</a>
              									</div>
              									<div class="content table-responsive table-full-width" style="min-height:auto;">
		 											<div class="table-responsive" style="border:none;" >
                        								<table class="table table-striped  table-bordered table-hover custom-tbl-st bt" id="table-breakpoint" style="background-color: rgb(255, 255, 255); border-radius: 4px;" >
															<thead>
																<tr>
		      														<th>Added by</th>
		      														<th>Last Modified</th>	      
			 														<th>Note</th>
			 														<th class="hidden-xs">Action</th>			  
		  														</tr>
															</thead>
		  													<tbody id="notesTable">
		  											
		  													</tbody>
		  												</table>
													</div>
												</div>
											</div>
										</div>
									</div>
									<% } %>
	  							</div>
	  							<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) { %>
              					<div class="chart tab-pane" id="third" >
              						<div CLASS="row" style="padding:15px;">
			<div class="clearfix hidden-xs">
				<div class="col-md-6" style="">
					<span class="display_records_count" style="font-weight:bold;"></span>
					
				</div>
				<div class="col-md-6 text-right">
      				<div class="form-group no-margin">
     					<div class="pull-right margin-bottom-10 ">
							<div class="form-inline" style="">                 	          
                 				<div class="btn-inline display-inline-block">
									<input class="form-control searchFieldClass" style="min-width:225px; height:36px;" required id="searchField" placeholder="Search here..." value="">
									<button class="btn btn-left-align btn-link no-shadow bg-transparent no-padding-top padding-right-10 right-auto left" type="button" id="searchBtn" title="Search"><i class="glyphicon glyphicon-search"></i></button>
								</div>
                				<a title="Add new task" href="<%= backendDirectory %>/task?project_id=<%= contentObj._id %>" target="_blank" class="btn btn-danger">Add new task</a>
                			</div>
         				</div>
      				</div>
				</div>
			</div>
				<div class="visible-xs" >
					<div class="col-md-12 display_records_count" style="font-weight:bold;"></div>
					<div class="col-lg-6 col-sm-12 col-xs-12" style="margin-bottom:10px;">
						<form class="form-inline" method="get" action=""> 
							<input class="form-control xs-margin-btm10 searchFieldClass" required id="smallSearchField" name="keyword" placeholder="Search..." type="text" value="">
                     	</form>
                     		<a title="Add new task" href="<%= backendDirectory %>/task?project_id=<%= contentObj._id %>" target="_blank" class="btn btn-danger">Add new task</a>
                	</div>
				</div>
			</div>
              						<div class="content table-responsive table-full-width" >
		 								<div class="table-responsive" style="border:none;">
                        					<table class="table table-striped  table-bordered table-hover custom-tbl-st bt" id="related_tasks" style="background-color: rgb(255, 255, 255); border-radius: 4px;" >
												<thead>
													<tr>
		      											<th>Task Name</th>
		      											<th>Start Date</th>
		      											<th>Est. Hours</th>
		      											<th>Details</th>			  
		  											</tr>
												</thead>
		  										<tbody id="related_tasks_data">
		  											
		  										</tbody>
		  									</table>
										</div>
									</div>
									<div style="text-align:center; display:none;" id="display_more_btn">
										<a style="width: 60%; margin-bottom: 10px;" class="btn palette-carrot btn-danger" title="Load more" onclick="load_more()" href="javascript:void(0);">Load more</a>
        							</div>
        							<div style="text-align:center" id="img_loading_div">
										Loading...<br /> <img id="imgAjaxLoading" src="img/loading.gif" width="109" height="25" style="border-width: 0px;">
									</div>
              					</div>
              					<% } %>
							</div>
          				</div>
					</section>
    			</div>
			</section>
		</div>
<!-- add_note_modal Starts Here-->
<% include include/add_note_modal %>
<!-- add_note_modal Ends Here-->
		<!-- Footer Starts Here-->
			<% include include/footer %>
		<!-- Footer Ends Here-->
	</div>
<script src="plugins/combobox/autocomplete-ui.js"></script>
<script src="js/moment.js"></script>
<script src="js/bootstrap-datetimepicker.min.js"></script>
<script src="js/jquery.validate.js"></script>
<!--<script src="js/ui/jquery-ui.js"></script>-->
<script src="js/basictable.js"></script>
<script language="javascript">
var table_row_id="<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) { %><%= contentObj._id %><% } %>";
var auth_user_id="<% if (typeof authenticatedUser._id !== 'undefined' && authenticatedUser._id !== null) { %><%= authenticatedUser._id %><% } %>";
var auth_user_name="<% if (typeof authenticatedUser.firstname !== 'undefined' && authenticatedUser.firstname !== null) { %><%= authenticatedUser.firstname %><% } %>";
	auth_user_name+="<% if (typeof authenticatedUser.lastname !== 'undefined' && authenticatedUser.lastname !== null) { %>&nbsp;<%= authenticatedUser.lastname %><% } %>";

var relatedCollectionStr='tasks', tableDisplayID='related_tasks', relatedFieldName='project_mongo_id', relatedFieldValue=table_row_id;
function draw_content_html (aaData) {
	var contentHtml="";
	$.each(aaData, function(i,row){
		contentHtml+='<tr><td>'+row.name+'</td><td>'+dateTimeFromUnix(row.start_timestamp, false)+'</td><td>'+row.task_estimated_hours+'</td><td><a href="'+backendDirectory+'/task?_id='+row._id+'" target="_blank"><i class="fa fa-pencil"></i></a></td></tr>';
	});
	return contentHtml;
}

	$(function () {
		var customer_id="";
	<% if (typeof queryStr !== 'undefined' && queryStr !== null && typeof queryStr.customer_id !== 'undefined' && queryStr.customer_id !== null && queryStr.customer_id !=="") { %>
		customer_id="<%= queryStr.customer_id %>";
	<% }else if(typeof contentObj.customer_mongo_id !== 'undefined' && contentObj.customer_mongo_id !== null)	{ %>
		customer_id="<%= contentObj.customer_mongo_id %>";
	<% } %>
		fetch_collection_autocomplete_list('customers', 'customer_mongo_id', customer_id);
	
		fetch_default_list("currency-list", "<% if (typeof contentObj.currency !== 'undefined' && contentObj.currency !== null) { %><%= contentObj.currency %><% } %>","currency");
		
		// validate form on keyup and submit
		$("#ProjectForm").validate({
			ignore: "",
			onkeyup: false,
			errorClass: 'error',
			validClass: 'valid',
			errorElement: "em",
			errorPlacement: function(error, element) {
				$(element).closest('div').append(error);
			},
			onfocusout: false,
			invalidHandler: function(form, validator) {
				var errors = validator.numberOfInvalids();
				if (errors) {                    
					validator.errorList[0].element.focus();
				}
			},
			rules: {
				name: { required: true },
				start_date: { required: true },
			},
			submitHandler: function(form) {
				if($("#id").val()!=""){
					generateNotesJson();
				}
				var startTimestampNum=return_timestamp_from_datetimepicker($('#start_date').val(), false);
				$('#start_timestamp').val(startTimestampNum);
				
				var startTimestampNum=return_timestamp_from_datetimepicker($('#deadline_date').val(), false);
				$('#deadline_timestamp').val(startTimestampNum);
				dataAsJson('ProjectForm', form);
 			}
		});			
		
            // datepicker plugin
            
            if($("#start_timestamp").val()!=""){
				var tempDisplayDate=return_datetimepicker_from_timestamp($("#start_timestamp").val());
				$("#start_date").val(tempDisplayDate)
			}
            $('#start_datetime_picker').datetimepicker({
				format: 'L',
				defaultDate:new Date()
			}).on("dp.show", function (e) {
				if($('#deadline_date').val()!==""){
				$('#start_datetime_picker').data("DateTimePicker").maxDate($('#deadline_date').val());
				}
        	}).on("dp.change", function (e) {
        		$('#deadline_datetime_picker').data("DateTimePicker").minDate(e.date);
        	});
			
			if($("#deadline_timestamp").val()!=""){
				var tempDisplayDate=return_datetimepicker_from_timestamp($("#deadline_timestamp").val());
				$("#deadline_date").val(tempDisplayDate)
			}
            $('#deadline_datetime_picker').datetimepicker({
				format: 'L'
			}).on("dp.show", function (e) {
				if($('#start_date').val()!=""){
				$('#deadline_datetime_picker').data("DateTimePicker").minDate($('#start_date').val());
				}
        	}).on("dp.change", function (e) {
        		$('#start_datetime_picker').data("DateTimePicker").maxDate(e.date);
            });
	});

//autocomplete
	var xhr;
  (function( $ ) {
    $.widget( "custom.combobox", {
      _create: function() {
        this.wrapper = $( "<span>" )
          .addClass( "custom-combobox" )
          .insertAfter( this.element );
 
        this.element.hide();
        this._createAutocomplete();
        this._createShowAllButton();
      },
 
      _createAutocomplete: function() {
        var selected = this.element.children( ":selected" ),
          value = selected.val() ? selected.text() : "";
 
        this.input = $( "<input>" )
          .appendTo( this.wrapper )
          .val( value )
          .attr( "title", "" )
          .addClass( "custom-combobox-input ui-widget ui-widget-content ui-state-default ui-corner-left" )
          .autocomplete({
            delay: 0,
            minLength: 0,
            source: $.proxy( this, "_source" )
          })
          .tooltip({
            tooltipClass: "ui-state-highlight"
          });
 
        this._on( this.input, {
          autocompleteselect: function( event, ui ) {
		  	//alert("show all");
            ui.item.option.selected = true;
			
            this._trigger( "select", event, {
              item: ui.item.option
            });
          },
 
          autocompletechange: "_removeIfInvalid"
        });
      },
 
      _createShowAllButton: function() {
        var input = this.input,
          wasOpen = false;
 
        $( "<a>" )
          .attr( "tabIndex", -1 )
          .attr( "title", "Show All Items" )
          .tooltip()
          .appendTo( this.wrapper )
          .button({
            icons: {
              primary: "ui-icon-triangle-1-s"
            },
            text: false
          })
          .removeClass( "ui-corner-all" )
          .addClass( "custom-combobox-toggle ui-corner-right" )
          .mousedown(function() {
            wasOpen = input.autocomplete( "widget" ).is( ":visible" );
          })
          .click(function() {
            input.focus();
 
            // Close if already visible
            if ( wasOpen ) {
              return;
            }
 
            // Pass last search string as value to search for, displaying last results
            input.autocomplete( "search", 'SHOWALL' );
          });
      },
 
      _source: function( request, response ) {
        //var matcher = new RegExp( $.ui.autocomplete.escapeRegex(request.term), "i" );
		var ele_select= this.element;
		if(request.term=='SHOWALL'){
			response(ele_select.children( "option" ).map(function() {
          var text = $( this ).text();
		 var value= $( this ).val();
          //if ( this.value && ( !request.term || matcher.test(text) ) )
            return {
              label: text,
              value: text,
              option: this
            };
        }) );
		
		}
		else
		{
		var jsonRow = backendDirectory+"/api_fetch_list?start=0&limit=20&collection=customers?s="+request.term;

		//alert(jsonRowURLStr);
		if(xhr) xhr.abort();
		xhr=$.getJSON(jsonRow,function(result){
			
			if(result.aaData.length>0){
				var html='<option value=""></option>';

				$.each(result.aaData, function(i,item)
				{
					html += '<option value="'+item._id+'">'+item.name+'</option>';
				});
				ele_select.html(html);
				
				
				response(ele_select.children( "option" ).map(function() {
          var text = $( this ).text();
		 var value= $( this ).val();
          //if ( this.value && ( !request.term || matcher.test(text) ) )
            return {
              label: text,
              value: text,
              option: this
            };
        }) );
				
				
			}
		});
       
	  } 
		
      },
 
      _removeIfInvalid: function( event, ui ) {
 
        // Selected an item, nothing to do
        if ( ui.item ) {

          return;
        }
 
        // Search for a match (case-insensitive)
        var value = this.input.val(),
          valueLowerCase = value.toLowerCase(),
          valid = false;
        this.element.children( "option" ).each(function() {
          if ( $( this ).text().toLowerCase() === valueLowerCase ) {
            this.selected = valid = true;	
			
            return false;
          }
        });
 
        // Found a match, nothing to do
        if ( valid ) {
          return;
        }
 
        // Remove invalid value
        this.input
          .val( "" )
          .attr( "title", value + " didn't match any item" )
          .tooltip( "open" );
        this.element.val( "" );
        this._delay(function() {
          this.input.tooltip( "close" ).attr( "title", "" );
        }, 2500 );
        this.input.data( "ui-autocomplete" ).term = "";
      },
 
      _destroy: function() {
        this.wrapper.remove();
        this.element.show();
      }
    });
  })( jQuery ); 
	</script>
<% if (typeof contentObj._id !== 'undefined' && contentObj._id !== null) { %>
	<script src="scripts/manage_notes.js"></script>
	<script src="scripts/list_by_collection.js"></script>
<%	}	%>
</body>
</html>
